services:
  frontend:
    image: lstz/reusableform:latest
    container_name: rfd_frontend
    restart: always
    ports:
      - '3301:3301'
    environment:
      - VITE_API_URL=http://rfd_api:3302
    networks:
      - rfd_network

networks:
  rfd_network:
    external: true
