services:
  frontend:
    image: lstz/reusableform:latest
    container_name: rfd_frontend
    restart: always
    ports:
      - '3301:3301'
    environment:
      - VITE_API_URL=https://api.reusableform.diy/api
    networks:
      - rfd_network

networks:
  rfd_network:
    external: true
